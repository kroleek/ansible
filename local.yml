---
- hosts: localhost
  connection: local
  become: true

  tasks:
    - name: install htop
      package:
        name: htop
    - name: install flux cli
      shell: curl -s https://fluxcd.io/install.sh | sudo bash
      become: true
